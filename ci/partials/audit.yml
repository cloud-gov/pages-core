platform: linux
inputs: [name: src]
outputs: [name: src]
params:
  AUDIT_DIR: .
  AUDIT_MAX_CRITICAL: 0
  AUDIT_MAX_HIGH: 0
run:
  dir: src
  path: bash
  args:
    - -c
    - |
      node ./ci/tasks/check-audit.js
