const expect = require("chai").expect

describe("Site model", () => {
  describe("before validate hook", () => {
    it("should lowercase the owner and repository values")
  })

  describe("after create hook", () => {
    it("should create a GitHub webhook for the site")
  })
})
