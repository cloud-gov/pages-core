const expect = require("chai").expect

describe("User model", () => {
  describe("validations", () => {
    it("should validate that an email is formatted properly if present")
    it("should require a username to be present")
  })
})
