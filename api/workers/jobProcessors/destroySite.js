const SiteDestroyer = require('../../services/SiteDestroyer');
const { logger } = require('../../../winston');

async function destroySite() {
  // const results = await SiteDestroyer.verifyRepos();
  // create some message with the results
  // log it
  // logger.info(msg.join('\n'));
}

module.exports = destroySite;
